"use client";
import React from "react";

export interface Comment {
    id: string;
    content: string;
    post_id: string;
}

const CommentCard:React.FC = () => {

    return (
        <div>

        </div>
    );
};

export default CommentCard;
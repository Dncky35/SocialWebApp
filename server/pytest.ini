[pytest]
# Use pytest-asyncio in auto mode for event loop compatibility (esp. Python 3.12+)
asyncio_mode = auto

# Add markers to avoid warnings (declare all your custom markers here)
markers =
    asyncio: mark test as asyncio test

# Verbose output for better visibility
addopts = -v
